#include <iostream>
#include <string>
using namespace std;

#include "Datagrama.h"
#include "Fila.h"
//#include "Rede.h"
//#include "Roteador.h"
//#include "TabelaDeRepasse.h"

int main()
{
    /* // Bateria de testes
    Fila* fila1 = new Fila(3);
    cout << fila1->getTamanho() << endl;
    Datagrama* teste1 = new Datagrama(1, 3, 4, "mensagem pip popa");
    Datagrama* teste2 = new Datagrama(1, 3, 4, "mensagem pip pope");
    Datagrama* teste3 = new Datagrama(1, 3, 4, "mensagem pip popi");
    Datagrama* teste4 = new Datagrama(1, 3, 4, "mensagem pip popo");
    Datagrama* teste5 = new Datagrama(1, 3, 4, "mensagem pip popu");
    cout << teste1->getDado() << endl;
    cout << teste1->getTtl() << endl;
    teste1->processar();
    cout << teste1->getTtl() << endl;
    cout << fila1->isEmpty() << endl;
    cout << fila1->enqueue(teste1) << endl;
    cout << fila1->enqueue(teste2) << endl;
    cout << fila1->enqueue(teste3) << endl;
    cout << fila1->enqueue(teste4) << " espera se 0" << endl;
    cout << fila1->enqueue(teste5) << " espera se 0" << endl;
    cout << fila1->isEmpty() << endl;
    */
    return 0;
}
